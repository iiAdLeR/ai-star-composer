# Optional: graduation ML track (after core API works)
# midiutil: كتابة MIDI من ml.generate_from_lstm (موجود أيضاً في requirements.txt الأساسي)
midiutil==1.2.1
numpy==2.3.0
scikit-learn==1.8.0
joblib==1.5.3

# External MIDI ingest (MAESTRO / Lakh / your licensed .mid files)
mido==1.3.3

# Sequence model (musical richness path)
torch==2.11.0

# Jupyter (optional — graduation notebook)
# ipykernel>=6.29
# jupyter>=1.0
